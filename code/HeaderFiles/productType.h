#ifndef PRODUCTTYPE_H
#define PRODUCTTYPE_H

// Enum representing all supported product categories
enum class productType {
    NON_PERISHABLE,
    PERISHABLE,
    CHEMICAL,
    FOOD,
    RAW_MATERIAL
};

#endif // PRODUCTTYPE_H
